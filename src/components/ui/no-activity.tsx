import React from "react";

export default function NoActivity({ disabled }: { disabled?: boolean }) {
  return <section className="flex h-96 w-full items-center justify-center border-t border-gray text-xl">No Activity Log</section>;
}
